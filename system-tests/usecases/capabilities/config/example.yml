programs:
  - name: some-caps
    path: target/path/harness
    args:
      - -o
      - system-tests/child-dump/some-caps.yml
    uid: 1000
    gid: 100
    pty: true
    capabilities:
      - CAP_KILL
      - CAP_NET_RAW

  - name: no-caps
    path: target/path/harness
    args:
      - -o
      - system-tests/child-dump/no-caps.yml
    uid: 1000
    gid: 100
    pty: true
