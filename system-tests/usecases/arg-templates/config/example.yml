programs:
  - name: program
    path: target/path/harness
    args:
      - -o
      - system-tests/child-dump/program.yml
      - "{{ ENV_VAR }} plus appendix"
      - "{{ ENV_VAR } plus appendix"
    env:
      - ENV_VAR: "hello world"

